# Google-Signin-demo

Esta carpeta contiene un pequeño frontend para obtener un token de google y probarlo con nuestor backend

RECORDAR reconstruir los modulos de node usando el comando en caso de querer probarlo localmente
````
npm install
```